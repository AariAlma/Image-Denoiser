if __name__ == "__main__":
    rng = np.random.default_rng(0)

    x = rng.random((64, 64))
    y = x + 0.05 * rng.standard_normal((64, 64))
    y = np.clip(y, 0, 1)

    print("PSNR:", psnr(x, y))
    print("SSIM:", ssim(x, y))
